"""
    Ce module contient:
        La classe roles qui est en fait un enum representant les
        roles des participants
"""

class Roles(object):
    """
        Enum permettant de representer le role
        d'un participant
    """
    Player = "player"
    Observer = "observer"
